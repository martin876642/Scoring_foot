# team_style_clustering — dépendances
# pip install -r team_style_clustering/requirements.txt

requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Optionnel : meilleure réduction 2D (fallback PCA si absent)
umap-learn>=0.5.3

# Optionnel : possession SofaScore (API instable — possession_override.csv recommandé)
# ScraperFC>=3.0.0
